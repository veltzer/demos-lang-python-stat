config_requires = []
dev_requires = []
install_requires = [
    "numpy",
    "matplotlib",
    "scipy",
]
build_requires = [
    "pymakehelper",
    "pydmt",
    "pyclassifiers",
]
test_requires = []
requires = config_requires + install_requires + build_requires + test_requires
